<figure class="min-w-0" aria-label="Illustrative Etch plan for a developer profile">
  <div class="flex items-center justify-between gap-4 border-b-2 border-base-content pb-4 font-mono text-xs tracking-widest uppercase">
    <span>Environment / 001</span>
    <span class="rounded-full border border-base-content px-3 py-1">Preview</span>
  </div>
  <div class="bg-base-200 p-5 sm:p-8">
    <div class="mb-8 flex items-start justify-between gap-4">
      <div>
        <p class="mb-2 font-mono text-xs uppercase tracking-widest">Profile</p>
        <p class="text-3xl font-bold tracking-tight">developer<span class="text-primary" aria-hidden="true">_</span></p>
      </div>
      <span class="font-mono text-4xl text-base-content/30" aria-hidden="true">~/</span>
    </div>
    <div class="border-y border-base-content/20">
      <div class="grid grid-cols-[1.5rem_minmax(0,1fr)] gap-3 py-5">
        <span class="font-mono" aria-hidden="true">+</span>
        <div class="min-w-0">
          <div class="flex flex-wrap items-center justify-between gap-2"><strong>git</strong><span class="font-mono text-xs">1 planned change</span></div>
          <p class="mt-2 break-all font-mono text-sm">files/gitconfig → ~/.gitconfig</p>
        </div>
      </div>
      <div class="flex items-center gap-3 border-t border-base-content/20 py-4 text-sm"><span class="font-mono" aria-hidden="true">→</span><span>Review first. Apply when ready.</span></div>
    </div>
    <div class="mt-7 space-y-3">
      <p class="font-mono text-xs uppercase tracking-widest">Nothing changed yet</p>
      <code class="block break-words text-sm">./etch plan --profile developer</code>
    </div>
  </div>
  <figcaption class="mt-3 flex justify-between gap-4 font-mono text-xs"><span>An illustrative plan</span><span>Your configuration, made explicit.</span></figcaption>
</figure>
