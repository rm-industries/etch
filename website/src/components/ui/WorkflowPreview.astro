<figure class="min-w-0" aria-label="Illustrative Etch plan for a developer profile">
  <div class="mockup-code w-full min-w-0 bg-base-300 text-base-content shadow-xl">
    <pre data-prefix="$"><code><span class="terminal-command">./etch</span> <span class="terminal-argument">plan</span> <span class="terminal-option">--profile</span> <span class="terminal-argument">developer</span></code></pre>
    <pre data-prefix=" "><code> </code></pre>
    <pre data-prefix=" "><code>Profile: <span class="terminal-argument">developer</span></code></pre>
    <pre data-prefix=" "><code> </code></pre>
    <pre data-prefix="+"><code>git · <span class="terminal-option">1 planned change</span></code></pre>
    <pre data-prefix=" "><code>files/gitconfig → ~/.gitconfig</code></pre>
    <pre data-prefix=" "><code> </code></pre>
    <pre data-prefix="→"><code>Review first. Apply when ready.</code></pre>
    <pre data-prefix=" " class="terminal-command"><code>Nothing changed yet.</code></pre>
  </div>
  <figcaption class="mt-3 flex justify-between gap-4 font-mono text-xs"><span>An illustrative plan</span><span>Your configuration, made explicit.</span></figcaption>
</figure>

<style>
  /* cspell:ignore oklch */
  /* Lower lightness without mixing in gray; preserve the theme hue and chroma. */
  .terminal-command {
    color: light-dark(oklch(from var(--color-success) calc(l * 0.7) c h), var(--color-success));
  }

  .terminal-option {
    color: light-dark(oklch(from var(--color-warning) calc(l * 0.7) c h), var(--color-warning));
  }

  .terminal-argument {
    color: light-dark(oklch(from var(--color-info) calc(l * 0.7) c h), var(--color-info));
  }

  pre {
    padding-right: 1rem;
    white-space: pre-wrap;
    overflow-wrap: anywhere;
  }

  pre::before {
    margin-right: 1rem;
  }
</style>
